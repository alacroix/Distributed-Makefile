//Lister nos noeuds
uniq $OAR_NODEFILE

//Switch entre les noeuds
oarsh nom_noeud

//Connexion grid
ssh jeanr@access.grid5000.fr

//Se mettre sur le site de Grenoble
ssh grenoble

//Résever un noeud
oarsub -I -l nodes=1,walltime=1:45 -t deploy

//Déploiement de l'environnement
kadeploy3 -f $OAR_NODE_FILE -e jessie-x64-big -k

//Connexion en admin au noeud
ssh root@griffon-42

//Configuration pour la connexion au net
export http_proxy=http://proxy:3128/ && export https_proxy=http://proxy:3128/

//clone
git clone https://github.com/alacroix/distributed-makefile.git

//cd dans le rep
cd distributed-makefile

//installation boost
apt-get install libboost-all-dev
